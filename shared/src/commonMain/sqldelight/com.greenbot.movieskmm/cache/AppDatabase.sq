CREATE TABLE Movie (
  id INTEGER NOT NULL,
  title TEXT,
  overview TEXT,
  posterPath TEXT,
  backDropPath TEXT
);

insertMovie:
INSERT INTO Movie(id, title, overview, posterPath, backDropPath)
VALUES(?,?,?,?,?);

removeAllMovies:
DELETE FROM Movie;

selectAllMovies:
SELECT Movie.* FROM Movie;